// ràng buộc kiểu dữ liệu trong 1 array nào đó

let x: [string, number, boolean?] = ['a', 1]

/**
 * index 0: phải là string
 * index 2: phải là number
 * index 3: phải là boolean hoặc không có giá trị
 */
